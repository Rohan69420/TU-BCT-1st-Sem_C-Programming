#include <stdio.h>
#include <conio.h>
#include <string.h>
int main()
{
    char str[20]
    printf("\nEnter a string to find the frequency");
    gets(str);
    for (int i=0;i<strlen(str);i++)
    for (int j=0;j<strlen(str);j++){
            
    }
    getch();
    return 0;
}